nota_1 = float(input('ingrese la primer nota: '))
nota_2 = float(input('ingrese la segunda nota: '))

nota = nota_1 * 0.40 + nota_2 * 0.60

print("la nota definitiva del estudiante es {} ".format(nota))

